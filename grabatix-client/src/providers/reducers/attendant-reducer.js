/** @format */

import {} from '../actions/attendant-actions'
const reducer = (state, action) => {
  const { type } = action
  switch (type) {
    default:
      return { ...state }
  }
}

export default reducer
